function uppercase(str) {
  return str.toUpperCase()
}

function firstLetterOfStr(str) {
  return str[0]
}

function concatStr(str, str2) {
  return str + str2
}

module.exports = { uppercase, firstLetterOfStr, concatStr }